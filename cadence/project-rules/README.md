# 项目个性化规则

本目录用于存放项目的个性化规则和模板，由 `cadence-init:project-rules-examples` skill 填充详细内容。

## 目录说明

- `cadence/project-rules/` — 用户自定义的规则和模板
- `.claude/rules/` — 框架内置规则（禁止直接修改）

## 注意事项

- 用户自定义规则只能存放在本目录
- 禁止在 `.claude/rules/` 目录中添加用户自定义规则
- 禁止直接修改 `.claude/rules/` 目录下的框架内置规则文件
